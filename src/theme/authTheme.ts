/**
 * Compat: paleta escura legada (mesmos tokens que o tema escuro atual).
 * Prefira `useTheme().colors` em telas novas.
 */
export type { AppColors } from "./palettes";
export { darkPalette as authColors } from "./palettes";
